name: Sachin Portfolio CICD

on:
  push:
    branches:
      - main
  workflow_dispatch: 

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  # ---------------------------------------------------------
  # STAGE 1: Git Leak Detection (Security Scan)
  # ---------------------------------------------------------
  git-leaks:
    name: Git Leaks
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 

      - name: Run Gitleaks Scan
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        continue-on-error: false 

  # ---------------------------------------------------------
  # STAGE 2: Build (Freeze Flask Application)
  # ---------------------------------------------------------
  build:
    name: BUILD
    needs: git-leaks 
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Freeze Flask App
        run: python freeze.py

      - name: Upload Build Artifacts
        uses: actions/upload-artifact@v4 
        with:
          name: portfolio-build
          path: build/ 

  # ---------------------------------------------------------
  # STAGE 3: Test (Basic Verification Checks)
  # ---------------------------------------------------------
  test:
    name: TEST
    needs: build 
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Download Build Artifacts
        uses: actions/download-artifact@v4 
        with:
          name: portfolio-build
          path: build/

      - name: Validate Build Output Structure
        run: |
          echo "Checking if main build files exist..."
          if [ ! -f "build/index.html" ]; then
            echo "CRITICAL ERROR: build/index.html missing!"
            exit 1
          fi
          echo "Build integrity check successful."

  # ---------------------------------------------------------
  # STAGE 4: Deploy (Pushing Compiled Static Files to main branch)
  # ---------------------------------------------------------
  deploy:
    name: DEPLOY
    needs: test 
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Download Compiled Artifacts
        uses: actions/download-artifact@v4 
        with:
          name: portfolio-build
          path: build/

      - name: Deploy to Main Branch
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: build
          branch: main # Changed from gh-pages to main branch
          git-config-name: github-actions[bot]
          git-config-email: 41898282+github-actions[bot]@users.noreply.github.com